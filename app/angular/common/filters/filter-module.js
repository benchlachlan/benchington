(function() {

    /**
    * @memberof bench.filter
    * @ngdoc module
    * @name Filter Module
    * @description
    *   A common filter module. Declare any modules for filters that are common to the entire application here.
    *   For example, a humanize filter would fit here.
    */
angular
    .module('bench.filter', []);

})();
