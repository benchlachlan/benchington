(function() {

angular
    .module('bench.config', [])
    .config(config);

    /**
    * @memberof bench.config
    * @ngdoc config
    * @name App Config
    * @description
    *   The global configuration component. Used to configure any providers for the app.
    */
    function config() {

    }

})();
