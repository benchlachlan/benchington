(function() {
    'use strict';

    /**
    * @memberof bench.home
    * @ngdoc module
    * @name Home Module
    * @description
    *   A module for the home component.
    *   Home gets a list of businesses from an API call declared in the resolve of it's route (found in core-module/app-routing.js).
    *   It then displays the information in a clean list.
    */
    angular
        .module('bench.home', [

        ]);
})();
