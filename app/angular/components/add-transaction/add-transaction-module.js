(function() {
    'use strict';

    /**
    * @memberof bench.add-transaction
    * @ngdoc module
    * @name Add Invoice Module
    * @description
    *   A module for the add transaction component.
    *   Add transaction creates a new transaction that gets stored locally but gets retrieved
    *   with the same API call as home
    */
    angular
        .module('bench.add-transaction', [

        ]);
})();
